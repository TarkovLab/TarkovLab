{
  "name": "tarkovlab",
  "version": "1.0.0",
  "private": true,
  "description": "TarkovLab landing page for Escape from Tarkov.",
  "scripts": {
    "start": "node server.js",
    "build": "rm -rf node_modules/typescript && npx -y --package typescript tsc",
    "watch": "rm -rf node_modules/typescript && npx -y --package typescript tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.5.2"
  }
}
